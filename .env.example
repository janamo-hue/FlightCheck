# The fare source is alaskaair.com, scraped with a headless browser, so it
# needs no API credentials. Only the email sender and report URL are secrets.
RESEND_API_KEY=
ALERT_EMAIL_TO=you@example.com
ALERT_EMAIL_FROM=alerts@yourdomain.com
# Optional: published GitHub Pages address, linked from every alert email.
REPORT_URL=
